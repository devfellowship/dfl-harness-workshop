# Problem context

There is a function `sum(numbers: number[]): number` in `problem/buggy-math.ts`
that should return the total of all numbers in the array. It currently returns
the wrong value for every input. The test file defines the expected behavior.

Your deliverable is a corrected copy of the file written to
`agent/solution/buggy-math.ts`.
