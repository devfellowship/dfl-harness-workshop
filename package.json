{
  "name": "dfl-harness-workshop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A hands-on kata: build an agent step by step, point it at a problem, eval it, publish, version, iterate — a tiny dfl-harness.",
  "scripts": {
    "step": "tsx",
    "test": "node --test --import tsx problem/buggy-math.test.ts",
    "eval": "tsx steps/07-eval/run.ts",
    "publish": "tsx steps/08-publish/run.ts"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^1.0.39",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ai": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.9.0"
  }
}
